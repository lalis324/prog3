
package matriz;

public class Operacion {
public static double[][] suma(double [f][c]matA, double [f][c] matB) {
    double[i][j] matR; 
     
        for (int i = 0; i < f; i++) {
            for (int j = 0; j < c; j++); 
                }

  
        for (int i = 0; i < f; i++) {
            for (int j = 0; j < c; j++);
                }

        for (int i = 0; i < f; i++) {
            for (int j = 0; j < c; j++) {
                matR[i][j] = matA[i][j] + matB[i][j];
            }
}
return matR;
}